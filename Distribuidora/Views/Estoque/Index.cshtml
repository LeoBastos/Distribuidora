@model IEnumerable<Distribuidora.Models.Estoque>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.QuantidadeEstoque)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ValorUnitario)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.QuantidadeEstoque)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ValorUnitario)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.EstoqueId }) |
            @Html.ActionLink("Details", "Details", new { id=item.EstoqueId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.EstoqueId })
        </td>
    </tr>
}

</table>
